server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wilterson-franco/config-server-repo
          search-paths:
            - station*
          repos:
            perf:
              pattern: '*/perf'
              uri: https://github.com/wilterson-franco/config-server-repo-perf
              search-paths:
                - station*
